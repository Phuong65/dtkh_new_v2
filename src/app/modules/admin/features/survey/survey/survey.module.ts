import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { SurveyRoutingRoutes } from './survey.routers';

@NgModule({
    declarations: [],
    imports: [
        CommonModule,
        SurveyRoutingRoutes
    ],
})
export class SurveyModule { }